# Copie para .env (não commitar) e preencha
# URL JDBC completa (sem senha aqui)
# Docker defaults
# - By default, docker-compose runs the backend with the H2 in-memory DB profile.
# - If you want to use Neon Postgres, set SPRING_PROFILES_ACTIVE=neon and fill NEON_*.

# SPRING_PROFILES_ACTIVE=h2

# Neon Postgres (optional)
# SPRING_PROFILES_ACTIVE=neon
NEON_JDBC_URL=jdbc:postgresql://<host>/<db>?sslmode=require
NEON_DB_USER=<user>
NEON_DB_PASSWORD=<password>
